import type="md:hidden"
            onClick={() => { Config } from "tailwindcss"; setIsMenuOpen(!isMenuOpen)}

export default {
	
          >
            <Menu className="h-darkMode: ["class"],5 w-5" />
          </Button
	content: [
		"./pages>
        </div>
      </div/**/*.{ts,tsx}",>
      
      {isMenuOpen &&
		"./components/**/*.{ts,tsx (
        <div className="m}",
		"./app/**/*.{ts,tsx}",
		d:hidden container py-4"./src/**/*.{ts, bg-background bordertsx}"
	],
	-t">
          <nav className="flex flex-col gap-prefix: "",
	theme: {4">
            <a href="#services" className="text-sm
		container: {
			center font-medium hover:text-primary">: true,
			padding: '
              Услуги
            </a>2rem',
			screens
            <a href="#process" className=": {
				'2text-sm font-xl': '1400px'medium hover:text-primary">
              
			}
		Процесс
            },
		extend:</a>
            <a href="#contact {
			colors: {
				border" className="text-sm font-medium hover:text-primary">: 'hsl(var(--
              Контакborder))',
				input: 'hты
            </a>
            sl(var(--input))',
				<a href="tel:+71234567890"ring: 'hsl(var(-- className="flex items-center gap-ring))',
				background: 'h2">
              <PhoneCall className="sl(var(--background))',
				foreground: 'h-4 w-4" />hsl(var(--foreground))',
              <span className="text-sm font-medium">+7 
				primary: {
					DEFAULT: (123) 456-78-90'hsl(var(--primary))',</span>
            </a>
          
					foreground: 'hsl(</nav>
        </div>
      )}var(--primary-foreground))'
    </header>
  );
};
				},
				secondary: {
					DEFAULT: 'h
